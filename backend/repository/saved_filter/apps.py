# Standard library
# (none needed)

# Third-party
from django.apps import AppConfig


class SavedFilterConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'repository.saved_filter'

